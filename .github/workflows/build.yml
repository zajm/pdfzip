name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:  # Allow manual trigger

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libegl1 \
            libxkbcommon0 \
            libxcb-cursor0 \
            libxcb-icccm4 \
            libxcb-keysyms1 \
            libxcb-shape0

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ".[dev]"

      - name: Build with PyInstaller
        run: |
          pyinstaller \
            --onefile \
            --windowed \
            --name PDFZip \
            --collect-submodules pdfzip \
            --hidden-import pdfzip.gui \
            --hidden-import pdfzip.gui.app \
            --hidden-import pdfzip.gui.main_window \
            --hidden-import pdfzip.gui.pdf_preview \
            --hidden-import pdfzip.gui.log_panel \
            --hidden-import pdfzip.core \
            --hidden-import pdfzip.core.interleaver \
            scripts/run_gui.py

      - name: Upload Linux artifact
        uses: actions/upload-artifact@v6
        with:
          name: PDFZip-linux
          path: dist/PDFZip

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ".[dev]"

      - name: Build with PyInstaller
        run: |
          pyinstaller `
            --onefile `
            --windowed `
            --name PDFZip `
            --collect-submodules pdfzip `
            --hidden-import pdfzip.gui `
            --hidden-import pdfzip.gui.app `
            --hidden-import pdfzip.gui.main_window `
            --hidden-import pdfzip.gui.pdf_preview `
            --hidden-import pdfzip.gui.log_panel `
            --hidden-import pdfzip.core `
            --hidden-import pdfzip.core.interleaver `
            scripts/run_gui.py

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v6
        with:
          name: PDFZip-windows
          path: dist/PDFZip.exe

  release:
    needs: [build-linux, build-windows]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - name: Download Linux artifact
        uses: actions/download-artifact@v6
        with:
          name: PDFZip-linux
          path: linux

      - name: Download Windows artifact
        uses: actions/download-artifact@v6
        with:
          name: PDFZip-windows
          path: windows

      - name: Get version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Rename artifacts with version
        run: |
          mv linux/PDFZip linux/PDFZip-${{ steps.version.outputs.VERSION }}-linux
          mv windows/PDFZip.exe windows/PDFZip-${{ steps.version.outputs.VERSION }}-windows.exe
          chmod +x linux/PDFZip-${{ steps.version.outputs.VERSION }}-linux

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: PDFZip ${{ steps.version.outputs.VERSION }}
          draft: false
          prerelease: false
          files: |
            linux/PDFZip-${{ steps.version.outputs.VERSION }}-linux
            windows/PDFZip-${{ steps.version.outputs.VERSION }}-windows.exe
          body: |
            ## PDFZip ${{ steps.version.outputs.VERSION }}

            PDF page interleaver with GUI and CLI support.

            ### Downloads
            - **Linux**: `PDFZip-${{ steps.version.outputs.VERSION }}-linux`
            - **Windows**: `PDFZip-${{ steps.version.outputs.VERSION }}-windows.exe`

            ### Installation via pip
            ```bash
            pip install pdfzip
            ```

            ### Usage
            ```bash
            # GUI mode
            pdf-zip

            # CLI mode
            pdf-zip -a odd_pages.pdf -b even_pages.pdf -o combined.pdf
            ```
